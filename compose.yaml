services:
  epoche:
    image: ghcr.io/asano69/epoche:latest
    container_name: epoche
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - epoche.env
    volumes:
      - ./data:/epoche/data
        #- /etc/ssl/certs/root_ca.crt:/certs/root_ca.crt:ro # optional
    environment:
      - TZ=Etc/UTC
      #
      - EPOCHE_SERVER_HOST=0.0.0.0
      - EPOCHE_SERVER_PORT=3000
      #networks:
    #  - traefik_net
    #labels:
    #  - traefik.enable=true
    #  - traefik.http.routers.epoche.rule=Host(`epoche.app.internal`)
#volumes:
#  data:

#networks:
#  traefik_net:
#    external: true

